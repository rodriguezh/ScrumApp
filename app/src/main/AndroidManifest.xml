<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="app.com.scrumapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.login.LoginActivity" />
        <activity
            android:name=".splash.SplashActivity"
            android:theme="@style/SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.historiausuario.HistoriaUsuarioActivity"
            android:label="@string/title_activity_historia_usuario"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.proyectos.MainProjectsActivity"
            android:label="@string/title_activity_main_projects"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".activities.Sprints.MainSprintsActivity"
            android:label="@string/title_activity_main_sprints"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>